<view class="container">
    <image src="{{img}}" class="banner {{isPlay?'rotate':''}}"></image>
    <image src="{{isPlay?play:stop}}" class="music" bind:tap="onMusic"></image>
    <image src="images/music.png" class="movie"></image>
    <text class="txt">{{content}}</text>
</view>